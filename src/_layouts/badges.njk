<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Overview</title>
    </head>
    <body>
        <h1>{{ title }}</h1>
        {% for badge in badges %}
          <a href="/badge/{{ badge.first }}-{{ badge.last }}-{{ badge.ticketid }}.html">{{ badge.first }}</a><br>
          {{ badge.conf }}<br>
          {{ badge.svg }}<br>
          {{ badge.color }}<br>
          {{ badge.last }}<br>
          {{ badge.company }}<br>
          {{ badge.ticketid }}<br><br>
        {% endfor %}
        </main>
    </body>
</html>